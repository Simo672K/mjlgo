package main

/*
	types in golang

	for numbers:
		int int32 int64
		float float32 float64
		uint unit32 uint64

	bytes:
		byte uint8

	string
	map -> map[string]int // or "make(map[string]int)"
	slice -> []int{} // this is how to declare a slice or "make([]int)"
	arrays // same as slice but with fixed size, here is how to declare it: [2]int{} 👈 array with two places
*/

// func main() {
// 	fmt.Println("Test 123")
// }
